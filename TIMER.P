DEF VAR X AS DECIMAL FORMAT ">>>,>>>,>>>,>>>".
DEF VAR Z AS DECIMAL FORMAT ">>>,>>>,>>>,>>>".
DEF VAR Y AS INTEGER.
DEF VAR W AS LOGICAL LABEL "TRY AGAIN ?".
BEG: REPEAT:
UPDATE Z LABEL "ENTER NUMBER YOU WISH TO COUNT TO".
X = 0.
Y = TIME.
REPEAT:
X = X + 1.
IF X = Z THEN LEAVE.
END.
DISPLAY TIME - Y LABEL "SECONDS TAKEN".
UPDATE W.
IF NOT W THEN LEAVE.
END.
